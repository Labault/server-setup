# Managed by server-setup — do not edit by hand (server setup overwrites it).
# Empty by default (D6, CDC §6.1 #11): server-setup does NOT touch the network
# stack unless you opt in with --paranoid, which swaps in the hardening variant
# (templates/sysctl/99-server-setup.paranoid.conf). We don't break a network by
# excess of zeal.
